# 리눅스에서 웹 서버 구축
```sh
dnf install httpd -y  // 툴 설치
systemctl start httpd // 툴 실행
[root@localhost home]# systemctl start httpd
[root@localhost home]# cd /var/www/html
[root@localhost html]# vi index.html
[root@localhost html]# ls -al


```